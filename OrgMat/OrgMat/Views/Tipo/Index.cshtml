@model List<OrgMat.Models.TipoModel>
@{
    ViewData["Title"] = "Index";
}

<div class="pagetitle">
    <h1 class="display-4"> Tipo de Material</h1>
</div>

<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <br />
            <div class="card-body">
                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                    <a class="btn btn-primary" role="button" asp-area="" asp-controller="Tipo" asp-action="Cadastrar"> Cadastrar novo </a>
                </div>
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">ID</th>
                            <th scope="col">Tipo</th>
                            <th scope="col">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.id_tipo</td>
                                <td>@item.tipo</td>
                                <td>
                                    <a class="btn btn-primary" href="Tipo/Visualizar/@item.id_tipo"><i class="bi bi-eye"></i></a>
                                    <a class="btn btn-success" href="Tipo/Editar/@item.id_tipo"><i class="bi bi-pencil"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>

    </div>
</div>

